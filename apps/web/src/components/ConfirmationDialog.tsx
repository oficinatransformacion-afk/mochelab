import { useEffect,useId,useRef,type ReactNode } from "react";
import { X } from "lucide-react";

export function ConfirmationDialog({title,description,icon,confirmLabel,busy=false,error="",danger=false,onClose,onConfirm,children}:{title:string;description:string;icon:ReactNode;confirmLabel:string;busy?:boolean;error?:string;danger?:boolean;onClose:()=>void;onConfirm:()=>void;children?:ReactNode}){
 const titleId=useId(),descriptionId=useId(),cancelRef=useRef<HTMLButtonElement>(null),dialogRef=useRef<HTMLElement>(null);
 useEffect(()=>{const previous=document.activeElement as HTMLElement|null;cancelRef.current?.focus();const handle=(event:KeyboardEvent)=>{if(event.key==="Escape"&&!busy){onClose();return}if(event.key!=="Tab")return;const focusable=[...(dialogRef.current?.querySelectorAll<HTMLElement>('button:not([disabled]),a[href],input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])')??[])];if(!focusable.length)return;const first=focusable[0],last=focusable[focusable.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}};document.addEventListener("keydown",handle);return()=>{document.removeEventListener("keydown",handle);previous?.focus()}},[busy,onClose]);
 return <div className="fixed inset-0 z-[100] grid place-items-center overflow-y-auto bg-slate-950/60 p-4" role="presentation" onMouseDown={event=>{if(event.target===event.currentTarget&&!busy)onClose()}}><section ref={dialogRef} role="dialog" aria-modal="true" aria-labelledby={titleId} aria-describedby={descriptionId} className="w-full max-w-lg overflow-hidden rounded-2xl border bg-white shadow-2xl"><div className="flex items-start gap-4 p-6"><span className={`grid h-11 w-11 shrink-0 place-items-center rounded-xl ${danger?"bg-rose-50 text-red-700":"bg-emerald-50 text-emerald-700"}`}>{icon}</span><div className="min-w-0 flex-1"><h2 id={titleId} className="text-xl font-bold">{title}</h2><p id={descriptionId} className="mt-1 text-sm text-slate-600">{description}</p></div><button type="button" disabled={busy} onClick={onClose} aria-label="Cerrar" className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 disabled:opacity-40"><X size={19}/></button></div><div className="px-6 pb-6">{children}{error&&<p role="alert" className="mt-4 rounded-xl bg-rose-50 p-3 text-sm font-semibold text-rose-900">{error}</p>}</div><div className="flex flex-col-reverse gap-2 border-t bg-slate-50 p-4 sm:flex-row sm:justify-end"><button ref={cancelRef} type="button" disabled={busy} onClick={onClose} className="rounded-xl border bg-white px-4 py-2.5 font-bold disabled:opacity-40">Cancelar</button><button type="button" disabled={busy} onClick={onConfirm} className={`rounded-xl px-4 py-2.5 font-bold text-white disabled:opacity-40 ${danger?"bg-red-600":"bg-emerald-600"}`}>{busy?"Procesando…":confirmLabel}</button></div></section></div>
}
