import{BadRequestException,Controller,Get,Headers,Param,Post}from"@nestjs/common";import{AdminOnly,RequirePermission}from"../access/access.decorators";import{AccessService}from"../access/access.service";import{JobsRepository}from"./jobs.repository";
@AdminOnly()@RequirePermission("MIGRACIONES","view")@Controller("jobs/admin")export class JobsController{constructor(private readonly repository:JobsRepository,private readonly access:AccessService){}@Get()list(){return this.repository.list()}@Post(":code/run")@RequirePermission("MIGRACIONES","edit")async run(@Param("code")code:string,@Headers("x-mochelab-demo-user-email")email?:string){if(!["RECONSTRUIR_RUTAS","RECALCULAR_OKR"].includes(code))throw new BadRequestException("Job no reconocido");return this.repository.run(code,await this.access.getUserId("ADMIN",email))}}
