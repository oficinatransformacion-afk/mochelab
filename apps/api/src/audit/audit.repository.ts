import { BadRequestException,Injectable } from "@nestjs/common";import { PrismaService } from "../database/prisma.service";
@Injectable()
export class AuditRepository{constructor(private readonly prisma:PrismaService){}private date(value:string|undefined,end=false){if(!value)return undefined;const date=new Date(value+(end?"T23:59:59.999":"T00:00:00.000"));if(Number.isNaN(date.getTime()))throw new BadRequestException("Fecha inválida");return date}async options(){const [entities,actions,results,origins]=await Promise.all([this.prisma.audit.findMany({distinct:["entity"],select:{entity:true},orderBy:{entity:"asc"}}),this.prisma.audit.findMany({distinct:["action"],select:{action:true},orderBy:{action:"asc"}}),this.prisma.audit.findMany({where:{result:{not:null}},distinct:["result"],select:{result:true},orderBy:{result:"asc"}}),this.prisma.audit.findMany({where:{origin:{not:null}},distinct:["origin"],select:{origin:true},orderBy:{origin:"asc"}})]);return{entities:entities.map(x=>x.entity),actions:actions.map(x=>x.action),results:results.flatMap(x=>x.result?[x.result]:[]),origins:origins.flatMap(x=>x.origin?[x.origin]:[])}}async list(q:{entity?:string;action?:string;result?:string;origin?:string;search?:string;from?:string;to?:string}){const from=this.date(q.from),to=this.date(q.to,true);if(from&&to&&to<from)throw new BadRequestException("La fecha final no puede ser anterior a la inicial");return this.prisma.audit.findMany({where:{entity:q.entity||undefined,action:q.action||undefined,result:q.result||undefined,origin:q.origin||undefined,occurredAt:from||to?{gte:from,lte:to}:undefined,OR:q.search?.trim()?[{actorLegacy:{contains:q.search.trim(),mode:"insensitive"}},{recordId:{contains:q.search.trim(),mode:"insensitive"}},{entity:{contains:q.search.trim(),mode:"insensitive"}},{action:{contains:q.search.trim(),mode:"insensitive"}},{user:{email:{contains:q.search.trim(),mode:"insensitive"}}},{user:{name:{contains:q.search.trim(),mode:"insensitive"}}}]:undefined},orderBy:{occurredAt:"desc"},take:1000,select:{id:true,occurredAt:true,user:{select:{email:true,name:true}},actorLegacy:true,action:true,entity:true,recordId:true,oldValue:true,newValue:true,result:true,origin:true}})}}
