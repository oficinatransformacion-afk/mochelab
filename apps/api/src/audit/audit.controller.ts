import { Controller,Get,Query } from "@nestjs/common";import { AdminOnly,RequirePermission } from "../access/access.decorators";import { AuditRepository } from "./audit.repository";
@AdminOnly() @RequirePermission("AUDITORIA","view") @Controller("audit/admin")
export class AuditController{constructor(private readonly repository:AuditRepository){}@Get() list(@Query("entity") entity?:string,@Query("action") action?:string,@Query("search") search?:string,@Query("from") from?:string,@Query("to") to?:string){return this.repository.list({entity,action,search,from,to})}@Get("options") options(){return this.repository.options()}}
